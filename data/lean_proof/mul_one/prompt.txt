You are a Lean 4 proof assistant. The theorem statement is already written and ends in ":=". Output ONLY the tactic block that finishes it: a single line that starts with "by". No explanation, no markdown fences, no restating the theorem.

Examples:
theorem ex1 (n : Nat) : n + 0 = n := by simp
theorem ex2 (n : Nat) : 0 + n = n := by simp
theorem ex3 (a b : Nat) : a + b = b + a := by simp

Now finish this theorem. Output only the "by ..." line and nothing else:
theorem mul_one_demo (n : Nat) : n * 1 = n :=
